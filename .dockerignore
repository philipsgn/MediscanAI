# Dependencies
node_modules
.venv
venv

# Build artifacts
.next
out
dist
__pycache__
*.pyc
*.pyo
*.tsbuildinfo
.pytest_cache

# Env & secrets (KHÔNG BAO GIỜ đóng gói .env vào image)
.env
.env.*
!.env.example

# IDE / OS
.vscode
.idea
.DS_Store
Thumbs.db

# Git
.git
.gitignore

# Misc
*.log
frontend_old
.antigravity
setup_windows.ps1

# [S4-Closeout] Benchmark data & artifacts — không cần trong build context
# (giảm context transfer từ ~672MB xuống mức tối thiểu; sample images chỉ
# dùng cho debug script phía host, không đóng gói vào image)
test-benchmark/
_v_s3_*.txt
backend/_v_s3_*.txt